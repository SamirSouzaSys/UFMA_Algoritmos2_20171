#include <stdio.h>
#include <time.h>

int main()
{
  int numberTests;
  char datesInit;
  struct tm strtime;


  scanf("%d",&numberTests);
  while(numberTests--)
  {
    scanf("%d",&datesInit);
    while(datesTests--)
    {
      scanf("%s",inputStr);
      strLength = strlen(inputStr);

      if(strLength > sizeStr){
        return 0;
      }

    }
  }
}